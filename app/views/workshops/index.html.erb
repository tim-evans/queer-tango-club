<% content_for :title, "Workshops | Queer Tango Club" %>

<section>
  <div class="container">
    <h2>Workshops</h2>
  </div>
  <% @workshops.each do |event| %>
    <div class="event">
      <div class="cover-photo"><img src="<%= event.cover %>" alt="<%= event.title %>"/></div>
      <div class="container">
        <div class="col end">
          <%= link_to event_path(event) do %>
            <h3 class="photo-overlay"><%= event.title %></h3>
          <% end %>
          <div>
            <h4 class="photo-overlay">
              <%= format_date_range(event.starts_at, event.ends_at) %><br>
              <% if event.location %><%= event.location.name %><% end %>
            </h4>
          </div>
          <div>
            <%= link_to cart_event_path(event), class: 'photo-overlay button with-icon' do %>
              <%= icon("ticket") %> Purchase Tickets
            <% end %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</section>
